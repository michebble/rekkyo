

<% @checklists.each do |checklist| %>
  <div>
    <h3><%= checklist.title %></h3>
    <%= link_to "Show", checklist %>
    <ul>
      <% checklist.tasks.each do |task| %>
        <li><span><%= task.action %><span></li>
      <% end %>
    </ul>
  </div>
<% end %>
